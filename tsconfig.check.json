{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "skipLibCheck": true
  },
  "include": ["src", "tests", "japaFile.ts"],
  "$comment": "Type checking covers the tests too; the build config emits only src. Modern resolution is needed because the test runner ships exports-only packages."
}
